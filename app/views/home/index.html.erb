<% flash.each do |name, msg| %>
  <%= content_tag :div, msg, :id => "flash_#{name}" if msg.is_a?(String) %>
<% end %>

<div class="row">
  <div class="span16">
    <div class="row">
      <div class="span6">
        <h2>Welcome to SongRake!</h2>

        <h4>
          <ol>
            <li>Add songs to request list</li>
            <li>Fast & Efficient</li>
            <li>Make hour holiday party a breeze & fun</li>
            <li>Keep your DJ in the know</li>
          </ol>
        </h4>
      </div>
      <div class="span10">
        <h3 id="signup_header">Sign up!</h3>
        <div id="signup">
          <%= simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
            <%= f.input :username %>
            <%= f.input :email %>
            <%= f.input :password %>
            <%= f.input :password_confirmation %>
            <div><%= f.button :submit, :class => "btn", :id => "singup_button"%></div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
